cuda:
  device_id: 0
  memory_limit_gb: 8
  enable_torch_compile: false
  precision: "auto"  # auto, fp16, bf16, fp32

cpu:
  thread_count: 4
  enable_multiprocessing: true
  max_workers: 4

venv:
  default: "venv_base"
  base: "venv_base"  # 基础服务和语音处理
  models: "venv_models"  # LLM和图像处理
  tts_compat: "venv_tts_compat"

# GPT-SoVITS配置
gpt_sovits:
  root_path: "d:\\AI\\xiaoyou-core\\models\\GPT-SoVITS-v2pro-20250604-nvidia50"
  device: "cuda"
  precision: "auto"